# Use the lightweight nginx image based on Alpine Linux
FROM nginx:alpine

# Copy your local index.html into the default nginx path
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 to allow traffic
EXPOSE 80